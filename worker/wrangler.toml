# Cloudflare Worker Configuration
# Copilot Academy - Kennistest API

name = "llm-kennis-api"
main = "gemini-proxy.js"
compatibility_date = "2024-01-01"

# Custom domain (optional)
# routes = [
#   { pattern = "api.test.copilot-academy.nl/*", zone_name = "copilot-academy.nl" }
# ]

# Environment variables (non-sensitive)
[vars]
ENVIRONMENT = "production"

# Secrets (set via wrangler secret put GEMINI_API_KEY)
# GEMINI_API_KEY = "your-api-key" # Don't put this here, use wrangler secret
